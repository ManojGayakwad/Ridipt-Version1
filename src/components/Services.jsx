import { Heading, Input, Button, Checkbox, Box } from "@chakra-ui/react";

import React, { useEffect, useState } from "react";


const Services = () => {
    return (
        <Box style={{
            background: 'radial-gradient(circle at center bottom, #1d665c, #1d252c 40%)', height: '100vh',
            width: '100vw',

            color: "white"
        }}>
            <Box  style={{position:"absolute"}} > 
            <h1>Service page</h1>
            </Box>
        </Box>
    );
};
export default Services;
